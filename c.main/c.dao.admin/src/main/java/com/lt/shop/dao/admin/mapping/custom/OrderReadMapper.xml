<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lt.shop.dao.admin.impl.custom.OrderReadMapper">
	


	<select id="list" resultType="java.util.Map">
		SELECT
		o.id,
		o.order_sn as orderSn,
		o.order_status as orderStatus,
		o.pay_status as payStatus,
		o.deliver_status as deliverStatus,
		o.amount,
		o.freight,
		o.receipt_name as receiptName,
		o.receipt_phone as receiptPhone,
		o.province,
		o.city,
		o.district,
		o.address,
		o.add_time as addTime,
		o.amount_refund as amountRefund,
		u.uname
		FROM
		lt_order o
		LEFT JOIN lt_user u on o.user_id=u.id
		<if test="params.w != null and params.w!=''">
			where o.order_sn like concat('%', #{params.w},'%')
			or 
			o.receipt_name like concat('%', #{params.w},'%')
			or 
			o.receipt_phone like concat('%', #{params.w},'%')
		</if>
		order by o.add_time desc
		LIMIT #{start},#{num}

	</select>
	<select id="listTotal" resultType="java.lang.Integer">
		SELECT
		count(0)
		FROM
		lt_order o
		LEFT JOIN lt_user u on o.user_id=u.id
		<if test="params.w != null and params.w!=''">
			where o.order_sn like concat('%', #{params.w},'%')
			or 
			o.receipt_name like concat('%', #{params.w},'%')
			or 
			o.receipt_phone like concat('%', #{params.w},'%')
		</if>
	</select>



</mapper>